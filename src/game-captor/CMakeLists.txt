NewTargetSource()
AddSourceFolder(RECURSE PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/public")
AddSourceFolder(RECURSE "${CMAKE_CURRENT_SOURCE_DIR}/private")
source_group(TREE ${PROJECT_SOURCE_DIR} FILES ${SourceFiles})

function(add_lib TARGET_NAME)
    set_target_properties(${TARGET_NAME} PROPERTIES FOLDER "hook")
    target_compile_features(${TARGET_NAME} PRIVATE cxx_std_20)

    if(WIN32)
        set_target_properties(${TARGET_NAME} PROPERTIES LINK_FLAGS_DEBUG "/NODEFAULTLIB:LIBCMT.LIB /NODEFAULTLIB:msvcrt")
        set_target_properties(${TARGET_NAME} PROPERTIES LINK_FLAGS_RELEASE "/NODEFAULTLIB:LIBCMTD.LIB /NODEFAULTLIB:msvcrtd")
    endif()

    target_link_libraries(${TARGET_NAME} PRIVATE inih::inihpp_a)

    AddTargetInclude(${TARGET_NAME})
    add_library(${PROJECT_NAME}::${TARGET_NAME} ALIAS ${TARGET_NAME})

    if(NOT OVERLAY_DISABLE_INSTALL)
        AddTargetInstall(${TARGET_NAME} ${PROJECT_NAME})
    endif()
endfunction()

set(TARGET_NAME game_captor)
add_library(${TARGET_NAME} SHARED ${SourceFiles})
add_lib(${TARGET_NAME})
target_compile_definitions(${TARGET_NAME} PRIVATE -DGAME_CAPTURE_EXPORTS)
target_link_libraries(${TARGET_NAME} PRIVATE libuv::uv)
target_link_libraries(${TARGET_NAME} PRIVATE UTILPP::simple_utilpp)
target_link_libraries(${TARGET_NAME} PRIVATE UTILPP::ipc_util)
target_link_libraries(${TARGET_NAME} PRIVATE UTILPP::windows_util)
target_link_libraries(${TARGET_NAME} PRIVATE UTILPP::simple_logger)
target_link_libraries(${TARGET_NAME} PUBLIC OverlayUtil::graphic_subsystem)
target_link_libraries(${TARGET_NAME} PUBLIC OverlayUtil::simple_hook_helper)

set(TARGET_NAME game_captor_a)
add_library(${TARGET_NAME} STATIC ${SourceFiles})
add_lib(${TARGET_NAME})
target_compile_definitions(${TARGET_NAME} PUBLIC -DGAME_CAPTURE_NODLL)
target_link_libraries(${TARGET_NAME} PRIVATE libuv::uv_a)
target_link_libraries(${TARGET_NAME} PRIVATE UTILPP::simple_utilpp_a)
target_link_libraries(${TARGET_NAME} PRIVATE UTILPP::ipc_util_a)
target_link_libraries(${TARGET_NAME} PRIVATE UTILPP::windows_util_a)
target_link_libraries(${TARGET_NAME} PRIVATE UTILPP::simple_logger_a)
target_link_libraries(${TARGET_NAME} PUBLIC OverlayUtil::graphic_subsystem_a)
target_link_libraries(${TARGET_NAME} PUBLIC OverlayUtil::simple_hook_helper_a)
